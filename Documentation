Confirm these three points before execution to ensure the macro runs successfully:

Model Readiness: The Calculation Sheet is fully updated and currently Open to allow the macro to interact with the live workbook.

Structural Alignment: The Setup tab mapping and the Test_Cases sheet headers are perfectly aligned to ensure data is pushed to the correct destinations.

Mapping Completeness: All required Inputs and Outputs for this specific run have been identified and mapped (non-NA) on the Setup tab.

Pushes test inputs into the model, captures static snapshots of results, and saves them as independent, value-only workbooks in the Verification folder.
